#include <cstdio>
#include <cstring>
#include <cmath>
#include <algorithm>
#include <climits>
#include <string>
#include <iostream>
#include <map>
#include <cstdlib>
#include <list>
#include <set>
#include <queue>
#include <stack>

using namespace std;

typedef long long LL;
const int maxn = 1005;
int cnt[maxn];


int main() {
    int n,t,k,m,T;
    scanf("%d",&T);
    while(T--) {
        memset(cnt,0,sizeof(cnt));
        scanf("%d%d%d%d",&n,&t,&k,&m);
        int nowtime = 0,nowkind = -1;
        for(int i = 1;i <= m;i++) {
            int hh,mm,tt,id,rcnt;
            scanf("%d:%d %d %d",&hh,&mm,&id,&rcnt);
            
        }
    }
    return 0;
}
